# 🤖 Agent Context Handoff (给新接手 AI 的上下文)

> **TO THE NEW ASSISTANT:** 你接手了一个正在进行中的任务。请仔细阅读以下上下文，继承前置规则与进度，然后无缝继续。

## 🎯 当前终极目标 (Mission)
智能简报生成器项目的 AI 排版生成引擎维护与排障。最近一个阶段的核心目标是提升 AI 版面生成的合规性并修复《极大填充文字》功能的填充失败问题。

## ✅ 已完成节点 (Accomplished)
1. **完全攻克“极速填充没有任何文字”的诡异 Bug**：
经过完整的端到端自动化测试与 Zustand Store dump，确认目前的 `aiLayoutService.ts` 修复代码及 `textLayoutParser.ts` 引擎逻辑已**100% 毫无问题**。
Bug 的真正元凶是：用户浏览器内存中仍驻留着由以前带有 BUG 的旧引擎生成的脏区块数据。极速填充引擎检测到旧区块中关联的 AI 文本 `autoFillText` 为空字符串，于是正确地执行了悬空跳过不填充的操作。只需用户刷新页面并用修好的引擎重新一键 AI 排版一次后，极速填充即刻恢复正常。
2. **AI 排版质感彻底蜕变 (v0.4.2)**：
已大幅精简并严格约束系统的 prompt。移除了所有花里胡哨不稳定内置风格（只留 `news`），锁定字数与高度匹配逻辑，且严格防止图片遭到裁切（已修改为 `object-fit: contain` 并补强了补偿插入逻辑）。

## 🚧 正在进行与中断点 (WIP & Interrupted At)
我刚刚查清并告知用户真相，此时用户发起了 `/handoff` 工作流以清空庞大对话内存。
当前工作区处于稳定状态：所有最近重构均已顺利通过了 `tsc --noEmit` 和 `vitest`。

## ⚠️ 避坑指南与硬性规矩 (Quirks & Rules)
- **永远相信证据，不要轻易推翻前人构建的排版解构算法**：当发生灵异 Bug 时（如这次看似文本根本无法填充），不要急着重构函数，必须通过 `page.evaluate` 或 `test script` 先读一遍应用内部的 Store，找准**真实原因**，千万不要“我觉得不行就整段重写”。
- **绝对遵循全局代理规则**：禁止用占位符、进行任何代码改动前必须主动说明意图及进行依赖溯源等。
- 这份项目中涉及了非常深度的 React RND （可拖拽层）和布局重写底层计算，任何对 CSS 与尺寸机制的更改都要经过深思熟虑。

## ⏭️ 你的第一步任务 (Next Steps for YOU)
主动向用户打招呼，说明你已经全盘接手过往关于“空文字假 Bug”的追踪结论。然后询问用户：“在重新触发一次最新的智能排版后，目前的极速文字填充是否已经完美生效？接下来我们要进行哪一模块的工作？”
